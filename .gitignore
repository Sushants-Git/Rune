# Build artifacts
.build/
build/
*.app
DerivedData/

# Vendored ghostty checkout (pinned via scripts/fetch-ghostty.sh)
vendor/ghostty/

# Zig
.zig-cache/
zig-out/

# Xcode's opinions about a SwiftPM package
.swiftpm/

# Stray artefacts other tooling drops in here, so `git add -A` can't sweep
# them into a commit again.
attestation.jws
ids.json

# macOS
.DS_Store

# Pinned fff C library (scripts/fetch-fff.sh)
vendor/fff/
