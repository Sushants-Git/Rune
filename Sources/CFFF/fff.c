// Declarations only. The library itself is loaded at run time — see FFF.swift —
// so this target exists to give Swift the header's types, and needs one
// source file to be a target at all.
#include "fff.h"
